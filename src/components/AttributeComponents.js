export const Label = (props) => (
    <div className="text-right font-bold">{props.children}</div>
)

export const ValueItalic = (props) => (
    <div className="text-left italic">{props.children}</div>
)

export const Value = (props) => (
    <div className="text-left">{props.children}</div>
)